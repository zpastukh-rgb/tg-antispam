# Local envs & deps (never needed in Railway build context from CLI snapshot)
.git
venv/
venv
.venv/
.venv-debug/
**/__pycache__/
**/*.pyc
webapp/node_modules/
webapp/dist/

# Тяжёлые файлы в корне репо (ТЗ PDF и т.п.) — не участвуют в Dockerfile.webapp, но раздувают CLI snapshot
*.pdf
**/*.pdf
avatar.PNG

# IDE / OS
.idea/
.vscode/
.cursor/
.DS_Store

# Env files (secrets come from Railway variables)
.env
.env.docker

*.log
